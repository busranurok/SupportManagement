@model WebUI.Models.TicketsTicketsByLastWeekViewModel

@{
    ViewData["Title"] = "Son Bir Hafta Açılan Kayıtlar";
}


@foreach (var item in Model.TicketsByLastWeek)
{
    @item.Id
    <br />
    @item.Subject
    <br />
    @item.Body
    <br />
}